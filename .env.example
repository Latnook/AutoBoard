# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Microsoft OAuth Credentials
# Get these from: https://entra.microsoft.com/
MICROSOFT_CLIENT_ID=your-entra-app-client-id
MICROSOFT_CLIENT_SECRET=your-entra-app-client-secret
MICROSOFT_TENANT_ID=your-entra-tenant-id

# NextAuth Configuration
# For development, use http://localhost:3000
# For production, use your actual domain (e.g., https://autoboard.company.com)
NEXTAUTH_URL=http://localhost:3000

# Generate a secure random secret using: openssl rand -base64 32
NEXTAUTH_SECRET=generate-a-random-secret-here

# n8n Webhook (Optional - for n8n workflow integration)
# Leave blank to use built-in API only
# NEXT_PUBLIC_N8N_WEBHOOK_URL=http://localhost:5678/webhook/autoboard-onboard
# NEXT_PUBLIC_N8N_USERNAME=your-n8n-username
# NEXT_PUBLIC_N8N_PASSWORD=your-n8n-password

# API Key (Optional - for n8n to call the unified API)
# Generate with: openssl rand -base64 32
# Only needed if you want n8n to call the API instead of duplicating logic
# API_KEY=your-generated-api-key-here
